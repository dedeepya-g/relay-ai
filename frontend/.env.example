# Copy to frontend/.env.local and adjust. Vite only exposes VITE_-prefixed vars.

# Base URL of the Relay backend. Point this at the Cloud Run URL once deployed.
VITE_API_BASE_URL=http://localhost:8080
